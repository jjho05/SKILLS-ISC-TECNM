# 5.1 Análisis y modelado de un sistema para su estudio mediante simulación

Esta sección integra todos los conocimientos adquiridos en el curso para desarrollar un proyecto completo de simulación. Es la culminación del aprendizaje y demuestra la capacidad de aplicar simulación a problemas reales del mundo industrial.

---

## Objetivo del Proyecto Integrador

Desarrollar un modelo de simulación completo de un sistema real que permita:
1. **Analizar** el comportamiento actual del sistema
2. **Identificar** cuellos de botella y problemas
3. **Proponer** mejoras basadas en evidencia
4. **Validar** las propuestas mediante simulación
5. **Comunicar** resultados a stakeholders

---

## Metodología de 7 Fases

### Fase 1: Definición del Problema

**Objetivo:** Entender claramente qué se quiere resolver.

**Actividades:**
1. **Identificar el sistema a estudiar**
   - ¿Qué proceso/sistema se va a simular?
   - ¿Por qué es importante estudiarlo?

2. **Definir objetivos específicos**
   - ¿Qué preguntas se quieren responder?
   - ¿Qué decisiones se tomarán con los resultados?

3. **Establecer alcance**
   - ¿Qué se incluye en el modelo?
   - ¿Qué se excluye (y por qué)?

**Ejemplo:**

```markdown
## Definición del Problema

**Sistema:** Línea de producción de ensamble de laptops

**Problema:** 
- Producción actual: 80 unidades/día
- Meta: 100 unidades/día
- Cuello de botella desconocido

**Objetivos:**
1. Identificar estación limitante
2. Evaluar impacto de agregar 1 operador
3. Determinar configuración óptima

**Alcance:**
- Incluye: 5 estaciones de ensamble, 2 inspecciones
- Excluye: Almacén de materias primas, empaque final
```

### Fase 2: Recolección de Datos

**Objetivo:** Obtener datos confiables del sistema real.

**Tipos de datos necesarios:**

**1. Tiempos de proceso:**
- Medir con cronómetro (100+ observaciones)
- Registros históricos del MES/ERP
- Estudio de tiempos y movimientos

**2. Tiempos entre llegadas:**
- Órdenes de producción
- Llegadas de clientes
- Fallas de equipo

**3. Recursos:**
- Número de operadores
- Número de máquinas
- Capacidades de almacenamiento

**4. Lógica del sistema:**
- Reglas de prioridad
- Políticas de inventario
- Turnos de trabajo

**Herramientas de recolección:**

```python
import pandas as pd
import numpy as np
from datetime import datetime

# Template para recolección de datos
datos_proceso = {
    'fecha': [],
    'estacion': [],
    'operador': [],
    'tiempo_inicio': [],
    'tiempo_fin': [],
    'tiempo_proceso': [],
    'unidades': [],
    'defectos': []
}

# Ejemplo de registro
datos_proceso['fecha'].append('2024-01-15')
datos_proceso['estacion'].append('Ensamble CPU')
datos_proceso['operador'].append('Juan P.')
datos_proceso['tiempo_inicio'].append('09:15:30')
datos_proceso['tiempo_fin'].append('09:22:45')
datos_proceso['tiempo_proceso'].append(7.25)  # minutos
datos_proceso['unidades'].append(1)
datos_proceso['defectos'].append(0)

# Convertir a DataFrame
df = pd.DataFrame(datos_proceso)
df.to_excel('datos_recolectados.xlsx', index=False)
```

**Análisis de datos:**

```python
# Estadísticas descriptivas
print("=== ANÁLISIS DE TIEMPOS DE PROCESO ===")
print(f"Media: {df['tiempo_proceso'].mean():.2f} min")
print(f"Desv.Est: {df['tiempo_proceso'].std():.2f} min")
print(f"Mínimo: {df['tiempo_proceso'].min():.2f} min")
print(f"Máximo: {df['tiempo_proceso'].max():.2f} min")

# Ajustar distribución
from scipy import stats

# Probar distribución normal
stat, p_value = stats.shapiro(df['tiempo_proceso'])
print(f"\nPrueba de normalidad: p-value = {p_value:.4f}")

if p_value > 0.05:
    print("→ Usar distribución Normal")
    mu = df['tiempo_proceso'].mean()
    sigma = df['tiempo_proceso'].std()
    print(f"   N({mu:.2f}, {sigma:.2f})")
else:
    print("→ Probar otras distribuciones (Lognormal, Weibull, etc.)")
```

### Fase 3: Construcción del Modelo

**Objetivo:** Traducir el sistema real a un modelo de simulación.

**Pasos:**

**1. Seleccionar herramienta:**
- SimPy (flexibilidad, gratis)
- Arena (visual, presentaciones)
- AnyLogic (sistemas complejos)

**2. Desarrollar modelo conceptual:**

```
[Llegada Órdenes] → [Cola] → [Estación 1] → [Estación 2] → [Inspección] → [Salida]
                                  ↓                ↓              ↓
                              [Operador 1]    [Operador 2]   [Inspector]
```

**3. Implementar modelo:**

```python
import simpy
import numpy as np

class LineaProduccion:
    """Modelo de línea de producción"""
    
    def __init__(self, env):
        self.env = env
        self.estacion1 = simpy.Resource(env, capacity=1)
        self.estacion2 = simpy.Resource(env, capacity=1)
        self.inspector = simpy.Resource(env, capacity=1)
        
        # Estadísticas
        self.unidades_producidas = 0
        self.tiempos_ciclo = []
    
    def proceso_orden(self, orden_id):
        """Proceso completo de una orden"""
        llegada = self.env.now
        
        # Estación 1
        with self.estacion1.request() as req:
            yield req
            tiempo_est1 = np.random.normal(7.5, 1.2)
            yield self.env.timeout(tiempo_est1)
        
        # Estación 2
        with self.estacion2.request() as req:
            yield req
            tiempo_est2 = np.random.normal(6.8, 1.0)
            yield self.env.timeout(tiempo_est2)
        
        # Inspección
        with self.inspector.request() as req:
            yield req
            tiempo_insp = np.random.normal(3.2, 0.5)
            yield self.env.timeout(tiempo_insp)
        
        # Completado
        tiempo_total = self.env.now - llegada
        self.tiempos_ciclo.append(tiempo_total)
        self.unidades_producidas += 1
    
    def generador_ordenes(self):
        """Genera órdenes de producción"""
        orden_id = 0
        while True:
            yield self.env.timeout(6)  # Una orden cada 6 minutos
            orden_id += 1
            self.env.process(self.proceso_orden(orden_id))

# Ejecutar simulación
env = simpy.Environment()
linea = LineaProduccion(env)
env.process(linea.generador_ordenes())
env.run(until=480)  # 8 horas = 480 minutos

print(f"Unidades producidas: {linea.unidades_producidas}")
print(f"Tiempo de ciclo promedio: {np.mean(linea.tiempos_ciclo):.2f} min")
```

### Fase 4: Verificación

**Objetivo:** Asegurar que el modelo funciona correctamente (sin errores de programación).

**Checklist de verificación:**

- [ ] **Entidades se crean correctamente**
  ```python
  print(f"Órdenes generadas: {orden_id}")
  print(f"Órdenes procesadas: {linea.unidades_producidas}")
  assert orden_id == linea.unidades_producidas, "¡Órdenes perdidas!"
  ```

- [ ] **Recursos se usan correctamente**
  ```python
  # Verificar que no hay deadlocks
  # Verificar que recursos se liberan
  ```

- [ ] **Tiempos son razonables**
  ```python
  assert all(t > 0 for t in linea.tiempos_ciclo), "Tiempos negativos!"
  assert all(t < 60 for t in linea.tiempos_ciclo), "Tiempos sospechosos!"
  ```

- [ ] **Estadísticas se recolectan**
  ```python
  assert len(linea.tiempos_ciclo) > 0, "No se recolectaron datos!"
  ```

**Técnica de debugging:**

```python
# Modo debug con prints
def proceso_orden_debug(self, orden_id):
    print(f"{self.env.now:.1f}: Orden {orden_id} llega")
    
    with self.estacion1.request() as req:
        yield req
        print(f"{self.env.now:.1f}: Orden {orden_id} inicia Est1")
        yield self.env.timeout(7.5)
        print(f"{self.env.now:.1f}: Orden {orden_id} termina Est1")
    
    # ... continuar
```

### Fase 5: Validación

**Objetivo:** Asegurar que el modelo representa el sistema real.

**Métodos:**

**1. Comparación con datos reales:**

```python
# Datos reales
unidades_reales_dia = 80
tiempo_ciclo_real = 18.5  # minutos

# Datos simulados
unidades_sim_dia = linea.unidades_producidas
tiempo_ciclo_sim = np.mean(linea.tiempos_ciclo)

# Comparar
error_produccion = abs(unidades_sim_dia - unidades_reales_dia) / unidades_reales_dia
error_tiempo = abs(tiempo_ciclo_sim - tiempo_ciclo_real) / tiempo_ciclo_real

print(f"Error en producción: {error_produccion*100:.1f}%")
print(f"Error en tiempo de ciclo: {error_tiempo*100:.1f}%")

if error_produccion < 0.10 and error_tiempo < 0.10:
    print("✓ MODELO VÁLIDO (error < 10%)")
else:
    print("✗ MODELO REQUIERE AJUSTES")
```

**2. Pruebas estadísticas:**

```python
from scipy import stats

# Recolectar datos reales de tiempos de ciclo
tiempos_reales = [18.2, 19.1, 17.8, 18.9, 17.5, ...]  # 30+ observaciones

# Prueba t
t_stat, p_value = stats.ttest_ind(tiempos_reales, linea.tiempos_ciclo)

print(f"Prueba t: p-value = {p_value:.4f}")
if p_value > 0.05:
    print("✓ No hay diferencia significativa")
else:
    print("✗ Hay diferencia significativa")
```

**3. Validación con expertos:**
- Presentar lógica del modelo
- Mostrar animación (si es posible)
- ¿Los resultados son creíbles?

### Fase 6: Experimentación

**Objetivo:** Probar escenarios "what-if" para tomar decisiones.

**Diseño de experimentos:**

```python
# Escenario 1: Configuración actual (baseline)
# Escenario 2: Agregar 1 operador en Estación 1
# Escenario 3: Agregar 1 operador en Estación 2
# Escenario 4: Reducir tiempo de inspección 20%

resultados = []

# Escenario 1: Baseline
env = simpy.Environment()
linea = LineaProduccion(env, config='baseline')
env.process(linea.generador_ordenes())
env.run(until=480)

resultados.append({
    'escenario': 'Baseline',
    'produccion': linea.unidades_producidas,
    'tiempo_ciclo': np.mean(linea.tiempos_ciclo),
    'costo': 0  # Sin cambios
})

# Escenario 2: +1 operador Est1
env = simpy.Environment()
linea = LineaProduccion(env, config='est1_extra')
env.process(linea.generador_ordenes())
env.run(until=480)

resultados.append({
    'escenario': '+1 Op Est1',
    'produccion': linea.unidades_producidas,
    'tiempo_ciclo': np.mean(linea.tiempos_ciclo),
    'costo': 25 * 8  # $25/hora × 8 horas
})

# ... continuar con otros escenarios

# Analizar resultados
df_resultados = pd.DataFrame(resultados)
print(df_resultados)

# Encontrar mejor escenario
df_resultados['beneficio'] = (df_resultados['produccion'] - 80) * 50  # $50 utilidad/unidad
df_resultados['beneficio_neto'] = df_resultados['beneficio'] - df_resultados['costo']

mejor = df_resultados.loc[df_resultados['beneficio_neto'].idxmax()]
print(f"\n✓ MEJOR ESCENARIO: {mejor['escenario']}")
print(f"  Producción: {mejor['produccion']} unidades")
print(f"  Beneficio neto: ${mejor['beneficio_neto']:.2f}")
```

### Fase 7: Documentación y Presentación

**Objetivo:** Comunicar resultados de manera efectiva.

**Estructura del reporte:**

```markdown
# Proyecto de Simulación: Línea de Producción XYZ

## 1. Resumen Ejecutivo
- Problema identificado
- Solución propuesta
- Resultados clave
- Recomendación

## 2. Introducción
- Contexto del problema
- Objetivos del estudio
- Alcance

## 3. Metodología
- Herramienta utilizada (SimPy)
- Datos recolectados
- Supuestos del modelo

## 4. Modelo de Simulación
- Diagrama conceptual
- Descripción de componentes
- Parámetros utilizados

## 5. Validación
- Comparación con sistema real
- Pruebas estadísticas
- Conclusión de validez

## 6. Resultados
- Escenarios evaluados
- Métricas de desempeño
- Análisis comparativo

## 7. Recomendaciones
- Configuración óptima
- Análisis costo-beneficio
- Plan de implementación

## 8. Conclusiones
- Hallazgos principales
- Limitaciones del estudio
- Trabajo futuro
```

**Visualizaciones clave:**

```python
import matplotlib.pyplot as plt

# Gráfico de comparación de escenarios
fig, axes = plt.subplots(1, 2, figsize=(14, 5))

# Producción por escenario
ax = axes[0]
ax.bar(df_resultados['escenario'], df_resultados['produccion'])
ax.axhline(y=100, color='r', linestyle='--', label='Meta')
ax.set_ylabel('Unidades Producidas')
ax.set_title('Producción por Escenario')
ax.legend()
ax.grid(True, alpha=0.3)

# Beneficio neto
ax = axes[1]
colors = ['green' if x > 0 else 'red' for x in df_resultados['beneficio_neto']]
ax.bar(df_resultados['escenario'], df_resultados['beneficio_neto'], color=colors)
ax.set_ylabel('Beneficio Neto ($)')
ax.set_title('Análisis Económico')
ax.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig('resultados_simulacion.png', dpi=300)
plt.show()
```

---

## Rúbrica de Evaluación

| Criterio | Puntos | Descripción |
|----------|--------|-------------|
| **Definición del problema** | 10 | Claridad de objetivos y alcance |
| **Recolección de datos** | 15 | Cantidad y calidad de datos |
| **Modelo conceptual** | 10 | Diagrama y descripción clara |
| **Implementación** | 20 | Código funcional y bien estructurado |
| **Verificación** | 10 | Evidencia de debugging y pruebas |
| **Validación** | 15 | Comparación con sistema real |
| **Experimentación** | 10 | Escenarios relevantes y bien diseñados |
| **Documentación** | 10 | Reporte completo y profesional |
| **TOTAL** | **100** | |

---

## Ejemplos de Sistemas Apropiados

### Manufactura
- Línea de ensamble
- Sistema de pintura
- Proceso de empaque
- Mantenimiento preventivo

### Servicios
- Banco (cajas)
- Hospital (emergencias)
- Call center
- Restaurante de comida rápida

### Logística
- Centro de distribución
- Sistema de transporte
- Almacén automatizado
- Puerto marítimo

### Retail
- Supermercado (cajas)
- Tienda departamental
- E-commerce (fulfillment)

---

## Checklist de Completitud

Antes de entregar, verificar:

- [ ] **Problema bien definido**
  - Objetivos claros
  - Alcance delimitado

- [ ] **Datos suficientes**
  - 100+ observaciones por variable
  - Distribuciones ajustadas

- [ ] **Modelo verificado**
  - Sin errores de lógica
  - Resultados razonables

- [ ] **Modelo validado**
  - Comparado con sistema real
  - Pruebas estadísticas aplicadas

- [ ] **Experimentos completos**
  - Al menos 3 escenarios
  - Múltiples réplicas (30+)

- [ ] **Documentación profesional**
  - Reporte completo
  - Gráficos de calidad
  - Código comentado

- [ ] **Presentación preparada**
  - Slides claros
  - Tiempo de 15-20 minutos
  - Respuestas a preguntas anticipadas

---

## Consejos Finales

**1. Comenzar temprano:**
- Recolección de datos toma tiempo
- Debugging puede ser largo
- Dejar tiempo para iteraciones

**2. Documentar todo:**
- Supuestos
- Decisiones de diseño
- Problemas encontrados

**3. Comunicar con stakeholders:**
- Validar supuestos
- Presentar avances
- Ajustar según feedback

**4. Ser realista:**
- No modelar TODO
- Enfocarse en lo importante
- Simplicidad es clave

**5. Preparar para preguntas:**
- ¿Por qué usaste esa distribución?
- ¿Cómo validaste el modelo?
- ¿Qué pasa si cambia X parámetro?

---

## Recursos Adicionales

**Templates:**
- Formato de recolección de datos
- Template de reporte
- Checklist de verificación

**Software:**
- SimPy: https://simpy.readthedocs.io
- Arena: Rockwell University
- Python: Pandas, NumPy, Matplotlib

**Bibliografía:**
- Law, A. (2015). "Simulation Modeling and Analysis"
- Banks, J. et al. (2010). "Discrete-Event System Simulation"

---

## Resumen

**Proyecto integrador = Aplicación completa de simulación**

**7 Fases:**
1. Definición del problema
2. Recolección de datos
3. Construcción del modelo
4. Verificación
5. Validación
6. Experimentación
7. Documentación

**Entregables:**
- Código fuente
- Reporte técnico
- Presentación
- Datos utilizados

**Éxito = Modelo válido + Recomendaciones accionables**

---

*Referencia: Programa SCD-1022 - TecNM*
*Este es el proyecto final que integra todas las competencias del curso.*
